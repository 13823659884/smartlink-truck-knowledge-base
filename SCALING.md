# 知识库扩容路线

当前知识库使用 SQLite FTS5 + 本地 Qdrant 中文向量的混合检索。SQLite 保存文档、会话、反馈和图谱；Qdrant 保存分块语义向量，小程序和网页接口保持不变。

## 当前阶段

保持 `retrieval.backend=qdrant_hybrid`：

1. 停止本地问答服务，把新资料放入语料目录后执行 `python .\scripts\build_kb.py`。
2. 构建脚本复用未变化文件的解析缓存，并只向量化新增或内容发生变化的分块。
3. 通过 `/api/performance` 观察 P95；不要只凭文件数量判断是否需要迁移。
4. 76 份扫描图纸已完成本地 OCR，60 份形成可检索分块；16 份无足够清晰文字，以 `image_only` 状态保留原图。新增扫描资料会在下次构建时自动增量 OCR。

## 当前 Qdrant 策略

- 中文语义模型：`BAAI/bge-small-zh-v1.5`，512维，本地 ONNX 推理。
- 关键词通道：SQLite FTS5，适合故障码、零件号和精确术语。
- 语义通道：Qdrant，适合“刹车不灵—制动效果降低”这类不同表达。
- 融合：RRF + 车型、场景和资料路径加权。
- 当前使用 Qdrant 本地持久化模式。达到数万至十万分块或需要多进程并发构建时，再迁移到独立 Qdrant Server；数据和接口结构无需改变。

## 何时试用 RAGFlow

RAGFlow 适合需要可视化文档解析、数据集管理、检索编排和团队运营后台的阶段，但它不是当前电脑上的轻量替换项。建议在独立服务器上试点，并满足 Docker、足够内存和磁盘空间后再部署。

试点应先导入一小批代表性文档，与当前系统对比：

- 扫描 PDF 的 OCR 与表格解析质量；
- 10 到 20 个真实维修问题的召回准确率；
- 冷查询 P95、并发能力和资源占用；
- 原文页码、图片预览及小程序返回字段能否保持一致。

只有这些指标明显优于当前方案时再迁移全量资料。RAGFlow 可作为后台解析和检索服务，豆包仍负责基于证据生成最终回答。
