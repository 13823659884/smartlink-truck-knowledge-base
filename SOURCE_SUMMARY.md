# 豆包多模态向量版源码汇总

## 当前主版本

项目当前默认交付为 `doubao-embedding-vision` 2,048 维多模态向量版本，入口为：

```powershell
.\scripts\start_vectorized.ps1
```

默认地址：`http://127.0.0.1:8009/`。

原 BGE 版本继续保留在 `scripts/serve.py` 和 8008 端口，仅用于兼容与效果对比。

## 已处理数据

- SQLite知识切片：95,252 条。
- 豆包文字向量：95,252 条。
- PDF页面视觉向量：21,840 条，来自210个PDF。
- 独立图片视觉向量：312条。
- 任务分类：95,252条。
- 结构化SPN/FMI：1,234条。
- 处理失败项：0。

## 功能范围

- PDF、Word、Excel、PPT与图片解析，支持OCR和内容哈希缓存。
- 文字、PDF页面与图片三个相互独立的Qdrant多模态集合。
- VIN、故障码、症状、用车、保养、保用、技术文件、图纸、案例和通用资料十类路由。
- SPN/FMI精确索引，输入SPN即可聚合同一参数下的全部FMI定义。
- SQLite FTS5、Qdrant语义、RRF、任务标签和知识图谱混合检索。
- 豆包快速问答、Kimi K3深度诊断、流式回答和连续追问。
- 原始文档、PDF页码、参考资料及反馈纠偏。
- 桌面端与移动端Web界面。

## 不上传的本地文件

原始资料、VIN业务数据、`output/`构建产物、Qdrant向量、解析缓存、OCR模型、日志和`.env`密钥均已加入`.gitignore`。GitHub仓库只保存可复现源码、配置示例和文字说明。
